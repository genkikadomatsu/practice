# LC 74 Search a 2D Matrix


class Solution:
    def searchMatrix(self, matrix: List[List[int]], target: int) -> bool:

        
        i, j = 0, 0

        # Find row-wise index
        top, bottom = 0, len(list - 1)
        i = top + bottom // 2

        while top <= bottom:
            
            i = top + bottom // 2


            if target > matrix[i][0]:
                if target <= matrix[i + 1][0]:
                    break
                i = top + 1
            
            elif target < matrix[i][0]:
                i = bottom - 1
            
            else:
                return True

        # Find the column-wise index
        left, right = 0, len(list[0] - 1)
        
        while left <= right:
            j = left + right // 2

            if target > matrix[i][j]:
                j = left + 1
            elif target < matrix[i][j]:
                j = right - 1
            else:
                return True
        
        return False
        